-#= link_to "Home", root_path, :class => 'brand'
%ul.nav
  %li
    = link_to "Apu", root_path, :class => 'brand'
  %li.dropdown
    %a.dropdown-toggle{"data-target" => "#", "data-toggle" => "dropdown", :href => "/page.html", :role => "button"}
      Profile
      %b.caret
    %ul.dropdown-menu{"aria-labelledby" => "drop1", :role => "button"}
      %li 
        -#= link_to 'Agenda', agendas_path
      -#- if user_signed_in? and current_user.admin?
      -#  %li 
      -#    = link_to 'Manage Users', users_path
      -#  %li.divider
      - if user_signed_in?
        %li
          = link_to 'Edit account', edit_user_registration_path
        %li.divider
        %li
          = link_to 'Logout', destroy_user_session_path, :method=>'delete'
      - else
        %li
          = link_to 'Login', new_user_session_path
  - if user_signed_in?
    %li.divider-vertical
    %li
      .navbar-text
        Ciao,
        %b=current_user.email


